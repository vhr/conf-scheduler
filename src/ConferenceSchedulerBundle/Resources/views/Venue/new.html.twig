{% extends 'base.html.twig' %}

{% set nav = 'venue' %}

{% block body %}
    <h1 class="mdl-layout-title">Venue creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('venue_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
