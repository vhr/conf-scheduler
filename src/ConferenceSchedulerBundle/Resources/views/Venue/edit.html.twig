{% extends 'base.html.twig' %}

{% set nav = 'venue' %}

{% block header %}
    {% include 'ConferenceSchedulerBundle:Hall:tabs.html.twig' with {'venue': venue, 'active': 'edit'} %}
{% endblock %}

{% block body %}
    <section class="mdl-layout__tab-panel is-active">
        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" class="mdl-button mdl-js-button mdl-button--colored" />
        {{ form_end(edit_form) }}

        <ul>
            <li>
                <a href="{{ path('venue_index') }}">Back to the list</a>
            </li>
            <li>
                {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
                {{ form_end(delete_form) }}
            </li>
        </ul>
    </section>
{% endblock %}
